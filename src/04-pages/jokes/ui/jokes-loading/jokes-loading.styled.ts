import { SharedUiCardSkeleton } from '@shared'
import styled from 'styled-components'

export namespace S {
    export const JokeSkeleton = styled(SharedUiCardSkeleton)`
        min-height: 300px;
    `
}
