import { styled } from 'styled-components'

export namespace S {
    export const PageJokes = styled.div`
        padding: 24px;
        display: flex;
        flex-direction: column;
        gap: 24px;
    `
}
